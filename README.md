## 1. procedure
sentence -> every words of sentence will be converted into vectors of given embedding dimensions -> using sentence update the hidden state -> using hidden state getting every BIO things.
